#include <stdio.h>
int main(){
 float a; // it can be any variable
 printf("Enter value: "); // it is just a message
 scanf("%f",&a);
 printf("Value=%f",a); //%f is used for floats instead of %d
 return 0;
}
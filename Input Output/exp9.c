#include <stdio.h>
int main( ) {
 char str[100];
 printf( "Enter a value :");
 gets( str ); // it is used alternate of scanf() function
 printf( "\nYou entered: ");
 puts( str ); // it is used alternate of printf
 return 0;
}
